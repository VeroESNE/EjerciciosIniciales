using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio4_1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

//Crea una aplicación que nos calcule el área de un círculo, cuadrado o triángulo. 
//Crea un método por cada figura para calcular cada área. 
//Cada uno de estos métodos tendrán como parámetros de entrada los datos necesarios (medidas) para calcular dichas áreas. 
//Cada método devolverá el resultado del área.
//Ejecuta estos métodos con valores de ejemplo desde el Start() y muestra el resultado que devuelven por consola.
//? Circulo: (radio ^ 2) * PI
//? Triangulo: (base * altura) / 2
//? Cuadrado: lado* lado
