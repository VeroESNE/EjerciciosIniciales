using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio4_3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Convertir()
    {

    }
}

//Crea un método llamado “Convertir” que sirva como conversor de euros a dólares y viceversa. 
//Tendrá dos parámetros de entrada: cantidad(tipo float) y moneda(tipo string).  
//El método será capaz de hacer la conversión en función al tipo de moneda introducido y  devolverá a su salida la conversión calculada. 
//Ejecuta este método con valores de ejemplo desde el Start() y muestra el resultado que devuelve por consola
