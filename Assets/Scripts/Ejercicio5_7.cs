using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio5_7 : MonoBehaviour
{
    [SerializeField] float jugador1;
    [SerializeField] float jugador2;
    [SerializeField] float jugador3;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

//En un combate por turnos de hasta 3 jugadores, se desea saber qué orden se va a establecer en sus turnos de ataque. 
//Atacará primero el de mayor velocidad.
//Para ello, desarrolla un programa que calcule el mayor de tres números enteros (las diferentes  velocidades) dados por el Inspector.
