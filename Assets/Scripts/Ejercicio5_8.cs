using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio5_8 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

//Desarrolla un programa que lea tres números enteros H, M, S por el Inspector que contienen hora, minutos y segundos respectivamente,
//y comprueba si la hora (con  minutos y segundos) que surge de su combinación es una hora válida.
//Por ejemplo, una  hora inválida sería: 25:89:13. Si el formato es correcto, muestra dicha hora por la  consola. 