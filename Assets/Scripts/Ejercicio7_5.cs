using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio7_5 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

//Programa que muestre los números desde 1 hasta numero (variable con valor positivo y serializada por el Inspector) utilizando la estructura while.
//Después, haz lo mismo utilizando la estructura for.