using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio7_7 : MonoBehaviour
{
    [SerializeField] int numero1 = 7;
    [SerializeField] int numero2 = 9;
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 0; i < length; i++)
        {

        }    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

//7.7.Programa que lea dos números serializados y muestre todos los números desde el menor hasta el mayor. 